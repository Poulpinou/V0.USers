<%= form_for(article) do |f| %>
  <% if article.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(article.errors.count, "error") %> prohibited this article from being saved:</h2>

      <ul>
      <% article.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <h3> <%= f.label "Label : " %>
    <%= f.select(:label, Article.labels_kind)%></h3>
  </div>


  <div class="field">
    <%= f.cktext_area(:content, id:"cka") %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>

  <script>
        CKEDITOR.replace('cka')
  </script>
<% end %>
